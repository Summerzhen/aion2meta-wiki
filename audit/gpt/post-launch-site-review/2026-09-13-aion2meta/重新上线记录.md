# Redeploy Record

Repository: https://github.com/summerzhen-gamesites/aion2meta-wiki

Commit:
- `f418b03` Add analytics binding and second-pass SEO fixes

GitHub Actions:
- Workflow: Deploy Worker
- Run: `34763599936`
- Status: success

Production checks:
- `https://aion2meta.wiki/` returns `200` with `server: cloudflare`.
- Homepage contains `google-site-verification`.
- Homepage contains `googletagmanager.com/gtag/js`.
- `https://www.googletagmanager.com/gtag/js?...` returns `200`.
- `https://aion2meta.wiki/sitemap.xml` returns `200`.
- Sitemap URL count: 49.
- Sitemap does not include `/404/`.

Notes:
- Website-side GA4 and GSC tags are deployed.
- Google Search Console property creation and sitemap submission require an authenticated Google session or OAuth token. No usable Google refresh token or `gcloud` login was available on this machine during this pass.

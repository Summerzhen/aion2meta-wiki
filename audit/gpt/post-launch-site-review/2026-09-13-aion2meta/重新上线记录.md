# Redeploy Record

Repository: https://github.com/summerzhen-gamesites/aion2meta-wiki

Commit:
- `f418b03` Add analytics binding and second-pass SEO fixes

GitHub Actions:
- Workflow: Deploy Worker
- Run: `34763599936`
- Status: success

Production checks:
- `https://aion2meta.wiki/` returns `200` with `server: cloudflare`.
- Homepage contains `google-site-verification`.
- Homepage contains `googletagmanager.com/gtag/js`.
- `https://www.googletagmanager.com/gtag/js?...` returns `200`.
- `https://aion2meta.wiki/sitemap.xml` returns `200`.
- Sitemap URL count: 49.
- Sitemap does not include `/404/`.

Notes:
- Google onboarding completed on 2026-09-14 using the private site-launch OAuth environment on `root@147.93.119.74`.
- GA4 property: `553950157`.
- GA4 web stream: `15771616710`.
- GA4 measurement ID: `G-R8TGM3TJER`.
- GSC property: `sc-domain:aion2meta.wiki`.
- GSC DNS verification: created and verified.
- GSC site add: `ok`.
- GSC sitemap submit: `ok`.
- GitHub Secrets updated: `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_GA_MEASUREMENT_ID`, `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`.
- Worker redeploy after secret update: GitHub Actions run `34797295271`, success.
- Final production check: homepage returns `200 cloudflare`, contains `G-R8TGM3TJER` and `google-site-verification`; sitemap returns 49 URLs and excludes `/404/`.

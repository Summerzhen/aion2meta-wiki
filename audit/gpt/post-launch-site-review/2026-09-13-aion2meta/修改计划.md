# Modification Plan

1. Add environment-driven Google bindings:
   - `NEXT_PUBLIC_GA_MEASUREMENT_ID`
   - `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`

2. Update the static generator:
   - Support `AION2_OUTPUT_DIR=public`.
   - Inject GA4 and Search Console tags in `<head>`.
   - Add `noindex,follow` on the 404 page.
   - Add table captions through reusable table generation and a page-level fallback.
   - Add ARIA state to tab controls.
   - Keep `CNAME`, sitemap and robots generation in the build path.

3. Update GitHub Actions:
   - Rebuild `public/` before Worker deploy.
   - Verify Google tags are present before deploying.
   - Keep Cloudflare Worker deployment as the only production path.

4. Improve search snippets:
   - Expand short descriptions for factions, gear progression, tier pages and Global vs Korea meta.

5. Redeploy and verify:
   - Confirm Worker deployment succeeds.
   - Confirm production HTML contains GA/GSC.
   - Confirm sitemap is clean.
